name: hive_generator
description: Extension for Hive. Automatically generates TypeAdapters to store any class.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  build: ^3.1.0
  collection: ^1.18.0
  source_gen: ^3.1.0
  hive:
    path: ../hive
  analyzer: ^8.1.1
  source_helper: ^1.3.8

dev_dependencies:
  test: ^1.26.3
  build_test: any
  build_runner: any
